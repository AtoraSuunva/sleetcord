import credentialsJSON from './storedCredentials.json'

export const Credentials = {
  token: credentialsJSON.token,
  testGuildID: credentialsJSON.testGuildID,
  clientID: credentialsJSON.clientID,
}
